services:
  - type: web
    name: TeamBridge
    runtime: static
    buildCommand: npm run build
    publishPath: dist
    envVars:
      - key: REACT_APP_BASE_URL
        value: https://teambridge-c991.onrender.com
    routes:
      - type: rewrite
        src: /(.*)
        dest: /